---
layout: post
title: "My First Post"
date: 2025-08-14 02:00:00 +0200 
description: "This is a brief summary." 
---

<p>This is my first post written entirely in <strong>HTML</strong>.</p>

<h2>Why Use HTML?</h2>
<p>Sometimes you need more control over the structure and styling that Markdown doesn't easily provide. For instance, if you want to embed custom scripts, specific `div` structures, or inline styles not supported by Markdown, using HTML directly is the way to go.</p>
<p>Sometimes you need more control over the structure and styling that Markdown doesn't easily provide. For instance, if you want to embed custom scripts, specific `div` structures, or inline styles not supported by Markdown, using HTML directly is the way to go.</p>
<p>Sometimes you need more control over the structure and styling that Markdown doesn't easily provide. For instance, if you want to embed custom scripts, specific `div` structures, or inline styles not supported by Markdown, using HTML directly is the way to go.</p>
<p>Sometimes you need more control over the structure and styling that Markdown doesn't easily provide. For instance, if you want to embed custom scripts, specific `div` structures, or inline styles not supported by Markdown, using HTML directly is the way to go.</p>
<p>Sometimes you need more control over the structure and styling that Markdown doesn't easily provide. For instance, if you want to embed custom scripts, specific `div` structures, or inline styles not supported by Markdown, using HTML directly is the way to go.</p>

<ul>
    <li>Item One</li>
    <li>Item Two</li>
    <li>Item Three</li>
</ul>

<div class="custom-box">
    This is a custom HTML div.
</div>
 
